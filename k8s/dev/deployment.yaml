apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1-dev
  namespace: app-1-dev
spec:
  replicas: 1
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: app-1-dev
  template:
    metadata:
      labels:
        app: app-1-dev
    spec:
      serviceAccountName: app-1-dev-sa
      # --- ADDED: Pod-level security context ---
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 2000
      containers:
        - name: app-1-dev
          image: <YOUR_ACCOUNT_ID>.dkr.ecr.us-east-1.amazonaws.com/dev/app-1:v1.0
          imagePullPolicy: Always
          # --- ADDED: Container-level security context ---
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
          ports:
            - containerPort: 80
          envFrom:
            - secretRef:
                name: app-1-dev-secrets
          resources:
            requests:
              cpu: "100m"
              memory: "250Mi"
            limits:
              cpu: "200m"
              memory: "350Mi"